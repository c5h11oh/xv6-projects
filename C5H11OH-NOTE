usys.S : 置入system call的macro
user.h : 定義system call / user library functions (ulib.c)的 signature
            help the C compilor to know what the system call should look like.
syscall.h : 定義syscall 代表的integer
traps.h : 可以看到硬體定義帶表不同trap的integer (我們只會用到system call, aka 64)
makefile.mk

kernel/bootmain.c
main.c
mainc()
tvinit()
idtinit() // idt initialization
trap()

vectors.S
trapasm.S // for alltraps()

trapframe is in x86.h

syscall.c

iret == interrupt return